============
Installation
============

At the command line::

    $ easy_install pingVisualizer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pingVisualizer
    $ pip install pingVisualizer
